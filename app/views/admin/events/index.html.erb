<p id="notice"><%= notice %></p>

<h1>Events</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Date</th>
      <th>Location</th>
      <th>Details</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.date %></td>
        <td><%= event.location %></td>
        <td><%= event.details %></td>
        <td><%= link_to 'Show', admin_event_path(event) %></td>
        <td><%= link_to 'Edit', edit_admin_event_path(event) %></td>
        <td><%= link_to 'Destroy', admin_event_path(event), method: :delete, data: {confirm: 'Are you sure' } %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New Admin Event', new_admin_event_path %>
 |
<%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>  before_action :authenticate_user!
